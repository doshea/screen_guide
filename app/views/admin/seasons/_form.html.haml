= form_for @season, url: url_for(controller: 'admin/seasons', action: :create) do |f|
  = f.label :number
  = f.number_field :number, value: ((@num_guess + 1) if @num_guess)
  = f.submit nil, class: 'button'