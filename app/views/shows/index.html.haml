%h1 All Shows
- if is_admin?
  %p= link_to 'Add Show', new_admin_show_path
%h4 Currently Airing
= render partial: 'shows_table', locals: {shows: @active}
%h4 Finished
= render partial: 'shows_table', locals: {shows: @inactive}
- if is_admin?
  %p= link_to 'Add Show', new_admin_show_path